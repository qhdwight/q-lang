pkg main {
    def void -> int32 {
        imp main {
            i32 x = 2;
            i32 y = x + 6;
            out y + x;
        }
    }
    def i32, i32 -> i32 {
        imp calculate(a, b) {
            out a + b;
        }
    }
}