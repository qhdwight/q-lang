pkg main {
    dat test {
        i32 { a; b; }
    }
    def void -> i32 {
        imp main {
            i32 { c := 2; }
            test { d := test{a=3, b=2, }; }
            c = 3 + 2 + 3;
            d.a = 5;
            d.b = c + 32;
            # i_pln d.a;
            # i_pln d.b;
            i_pln c;
            i_pln d.a;
            i_pln d.b;
            out 0;
        }
    }
}
