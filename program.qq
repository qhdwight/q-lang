pckg main {
    def void -> int {
        impl main {
            int32 x = 2;
            int32 y = x + 6;
            out y + x;
        }
    }
    def int, int -> int {
        impl calc(a, b) {
            out a + b;
        }
    }
}