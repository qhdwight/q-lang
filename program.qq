pkg main {
    dat test {
        i32 a;
    }
    def void -> i32 {
        imp main {
            test {
                a := {};
            }
            i32 {
                b := 2 + 3;
                c := 4 + b + b;
            }
            i_pln c;
            out 0;
        }
    }
    # def i32, i32 -> i32 {
    #     imp calculate(_a, _b) {
    #         out _a + _b;
    #     }
    # }
}
